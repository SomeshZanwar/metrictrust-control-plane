services:
  - type: web
    name: metrictrust-control-plane
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash scripts/run_dbt.sh && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
